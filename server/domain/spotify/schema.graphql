scalar ComingSoon

type SpotifyTrack {
  cover: String!
  title: String!
  artist: String!
  listenedOn: String!
}

type Spotify {
  history: [SpotifyTrack]
}

extend type User @key(fields: "id") {
  id: ID!
  spotify: Spotify
}