scalar Date

type InstagramAccount {
  name: String!
  url: String!
  followingSince: Date!
}

type Instagram {
  following: [InstagramAccount]
}

extend type User @key(fields: "id") {
  id: ID!
  instagram: Instagram
}
